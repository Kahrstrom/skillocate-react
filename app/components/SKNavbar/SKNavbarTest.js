import React from 'react';
import { AppBar } from 'react-toolbox';
import theme from './style.scss';

const SKNavbarTest = () => {
    return (
        <AppBar
            title="Skillocate"
            theme={theme}
        />
    );
};

export default SKNavbarTest;
